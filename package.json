{
  "name": "digitalsmod-link-bypass",
  "version": "2.0.0",
  "description": "Backend theo dõi redirect chain và trích xuất URL đích công khai từ các trang shortener (hiện hỗ trợ link4m.*). Không giải captcha, không đánh cắp cookie, không giả lập kết quả.",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/"
  },
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0"
  }
}
